## Firebase Custom Domain Verification – Cloudflare DNS Records Fix

Below are the exact DNS CNAME records you must create (or update) in Cloudflare for the custom domain **www.shinerva.id**. Copy‑paste each block verbatim.

---
### RECORD 1
- **Type:** CNAME
- **Name:** `firebase1._domainkey.www`
- **Target Value:** `v=DKIM1; k=rsa; p=PLACEHOLDER_FOR_FIREBASE_DKIM_PUBLIC_KEY_1`

> **⚠️ Action Required:** Replace `PLACEHOLDER_FOR_FIREBASE_DKIM_PUBLIC_KEY_1` with the full DKIM public‑key string provided in the Firebase console. It will be a long string beginning with `v=DKIM1; k=rsa; ...`.

---
### RECORD 2
- **Type:** CNAME
- **Name:** `firebase2._domainkey.www`
- **Target Value:** `v=DKIM1; k=rsa; p=PLACEHOLDER_FOR_FIREBASE_DKIM_PUBLIC_KEY_2`

> **⚠️ Action Required:** Replace `PLACEHOLDER_FOR_FIREBASE_DKIM_PUBLIC_KEY_2` with the second DKIM public‑key string shown in the Firebase console.

---
## Important Cloudflare Settings
- **Proxy Status:** **DNS Only** (gray cloud) for **both** records. The proxy must be disabled so Firebase's verification scanner can directly read the CNAME entries.
- **TTL:** Set to **Auto** or the lowest possible value.
- **Propagation:** After saving, allow a few minutes for DNS propagation before re‑running the Firebase verification.

---
### Quick Checklist
1. Add/Update the two CNAME records exactly as shown.
2. Ensure **Proxy** is set to **DNS Only** (gray cloud) for each.
3. Verify the long DKIM strings are correctly copied with no extra whitespace.
4. Save changes and return to Firebase console → **Custom Domains** → **Verify**.

If you encounter further errors, double‑check that there are no trailing periods in the **Name** fields and that the **Target Value** strings are complete.

---
*Generated by Antigravity – your automated full‑stack and CLI assistant.*
